# AI-Document-Information-Extractor
"A computer vision and OCR application built with Python, Tesseract, and Pillow. It automates text extraction from unstructured document images (invoices, receipts, forms) and uses regular expressions (Regex) to parse and isolate structured data fields like email addresses.
